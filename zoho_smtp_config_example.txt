# Opción 2: Usar servidor SMTP de s-tech.com.pe
# (Requiere obtener credenciales del servidor de correo)

MAIL_MAILER=smtp
MAIL_HOST=smtp.s-tech.com.pe  # O el servidor SMTP que use Zoho para este dominio
MAIL_PORT=587
MAIL_USERNAME=noreply@s-tech.com.pe  # O crear esta cuenta
MAIL_PASSWORD=password_del_servidor
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@s-tech.com.pe
MAIL_FROM_NAME='Stech Peru - Sistema'

# Si usa Zoho Mail Business, típicamente es:
# MAIL_HOST=smtp.zoho.com
# MAIL_PORT=587 (TLS) o 465 (SSL)
# MAIL_ENCRYPTION=tls o ssl
